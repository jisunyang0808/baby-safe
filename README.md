# 아이세이프 (BabySafe) 🛡️

육아용품 유해 성분 및 리콜 정보 플랫폼

## 프로젝트 구조

```
baby-safe/
├── app/
│   ├── components/          # 재사용 UI 컴포넌트
│   │   ├── Header.tsx       # 헤더 (네비게이션)
│   │   ├── Footer.tsx       # 푸터
│   │   ├── RecallCard.tsx   # 개별 리콜 카드
│   │   ├── RecallList.tsx   # 리콜 목록 (필터링 포함)
│   │   ├── FilterBar.tsx    # 검색/필터 바
│   │   ├── StatCard.tsx     # 통계 카드
│   │   └── AlertBanner.tsx  # 긴급 알림 배너
│   │
│   ├── lib/
│   │   ├── mock-data.ts     # 목업 데이터 (API 연동 전)
│   │   └── utils.ts         # 유틸 함수, 설정 상수
│   │
│   ├── types/
│   │   └── index.ts         # TypeScript 타입 정의
│   │
│   ├── recalls/
│   │   └── page.tsx         # 리콜 목록 페이지
│   │
│   ├── ingredients/
│   │   └── page.tsx         # 유해 성분 사전 페이지
│   │
│   ├── layout.tsx           # 루트 레이아웃
│   ├── page.tsx             # 메인 홈 페이지
│   └── globals.css          # 글로벌 스타일
│
├── public/
├── next.config.mjs
├── tailwind.config.ts
├── tsconfig.json
└── package.json
```

## 주요 기능

| 페이지 | 경로 | 설명 |
|--------|------|------|
| 메인 | `/` | 최신 리콜 뉴스, 통계, 사이드바 |
| 리콜 현황 | `/recalls` | 전체 리콜 목록 + 필터링 |
| 유해 성분 | `/ingredients` | 성분 사전 (위험도, 규정) |
| 제품 검색 | `/search` | 브랜드/제품명 검색 (추후 구현) |
| 안전 가이드 | `/guide` | 연령별 안전 체크리스트 (추후 구현) |

## 시작하기

```bash
npm install
npm run dev
```

## 데이터 소스 (추후 연동)
- 한국소비자원 위해정보 API
- 식품의약품안전처 OpenAPI  
- 국가기술표준원 제품안전 정보

## 기술 스택
- **Framework**: Next.js 14 (App Router)
- **Styling**: Tailwind CSS
- **언어**: TypeScript
- **아이콘**: Lucide React
- **폰트**: DM Serif Display + Noto Sans KR
